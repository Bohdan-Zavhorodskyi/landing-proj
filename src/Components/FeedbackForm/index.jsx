import React from "react";
import useStyles from "./styles";

const FeedbackForm = () => {
  const classes = useStyles();
  return <div className={classes.root}>Feedback form</div>;
};

export default FeedbackForm;
