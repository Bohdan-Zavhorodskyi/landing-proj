import React from "react";
import useStyles from "./styles";

const Portfolio = () => {
  const classes = useStyles();
  return <div className={classes.root}>Page with all from portfolio</div>;
};

export default Portfolio;
